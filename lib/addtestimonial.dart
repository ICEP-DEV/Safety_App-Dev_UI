import 'package:flutter/material.dart';

class AddTestimonials extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Add Testimonial"),
        centerTitle: true,
        backgroundColor: Colors.red[700],
      ),
    );
  }
}
